# ADR-0006 — Initialize / Update Workflow

## Decision
The project exposes only two lifecycle operations: initialize and update.

## Why
Initialization creates the knowledge base. Updates synchronize only what changed.
