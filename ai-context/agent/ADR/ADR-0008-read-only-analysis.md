# ADR-0008 — Read-Only Repository Analysis

## Decision
AI Context Generation never modifies production code. Only the `AI-Context` directory may be created or updated.

## Why
The project is a documentation and knowledge-generation workflow.
